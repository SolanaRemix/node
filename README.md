## 🚀 Elite CLI Commands

Use comment triggers in PRs or issues to invoke workflows:

- 🚀 `@repair` → Run Atomic Node Repair
- 🧹 `@clean` → Clean entropy (node_modules, lockfiles)
- 🔒 `@lock` → Enforce frozen lockfile install
- 📦 `@build` → Verify build
- ✅ `@test` → Run test suite
- 📖 `@docs` → Refresh docs folder
- 📝 `@changelog` → Update changelog automatically

### Supported Node Versions
- Production: **20.x + 22.x**
- Development: **18.x → 24.x**
